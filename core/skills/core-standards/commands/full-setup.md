---
description: Set up any project for autonomous production development. Detects stack, generates CLAUDE.md, .claude/rules/, agent files, craft.json, enforcement.json, and wires hooks. Run once in a new or existing project.
---

# /full-setup

$ARGUMENTS

Read and execute the `core-standards:full-setup` skill exactly as specified.

Pass `$ARGUMENTS` as the project description if provided. If no arguments given, detect the project from existing files.
